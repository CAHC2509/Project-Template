public enum States
{
    Main,
    Onboarding,
    Gameplay,
    Results
}

public enum SettingsSubmenuType
{
    Graphics,
    Audio,
    Input,
    Localization,
    Close
}

public enum SaveSystemType
{
    PlayerPrefs,
    JSON,
    CSV
}
